const sessionConfig = {
  /**
   * For extra verbose logging!
   */
  debug: false,
  /**
   * When this is enabled, only fetch the user
   * from the default database and
   * do not hit the GitHub API
   */
  dryRun: false,
}

export default sessionConfig
